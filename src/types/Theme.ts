type Theme = {
   colors: {
      primary: string;
      primaryDark: string;
      primaryLight: string;
      secondary: string;
      background: string;
      text: string;
      alert: string;
      success: string;
      danger: string;
   }
}

export default Theme