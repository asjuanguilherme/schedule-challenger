type Theme = {
   colors: {
      primary: string;
      secondary: string;
      background: string;
      text: string;
      alert: string;
      success: string;
      danger: string;
   }
}

export default Theme